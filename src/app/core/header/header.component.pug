div.header.space-between
  div.my-container--center
    mat-icon(matTooltip='Menu') menu
    h4.title Shop
  div
    mat-icon(matTooltip='Shopping list') shopping_basket
    mat-icon(matTooltip='Reciples') room_service
    mat-icon(matTooltip='Settings') settings
    mat-icon(matTooltip='Exit') exit_to_app